# managesieve service is defined in 10-master.conf. Dovecot 2.3 does not
# support a `protocol sieve { }` block, so we intentionally leave this file
# minimal — the values below would go into a `plugin { }` block, and we
# already declare sieve plugin settings in 90-sieve.conf.
