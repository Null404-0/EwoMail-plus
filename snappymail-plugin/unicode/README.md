# UNICODE SnappyMail Plugin

## Cloudflare Turnstile 配置
1. 访问 `https://dash.cloudflare.com/` 并登录。
2. 打开 **Turnstile** → **Add Site**。
3. 录入当前邮局域名（如 `mail.example.com`）。
4. 创建后获得 `Site Key` 和 `Secret Key`。
5. 到 EwoMail 管理后台：系统管理 → 系统设置 → Turnstile，填入并保存。
